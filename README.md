# k-clustering
Project Title: K-Means Clustering on the Iris Dataset Description:  Implemented K-Means Clustering — an unsupervised machine learning algorithm — on the Iris dataset to discover natural groupings in flower measurement data without any labels. Used the Elbow Method to intelligently determine the optimal number of clusters (K=3), 
